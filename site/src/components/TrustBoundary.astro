---
/**
 * TrustBoundary — 三栏信任边界展示。
 *
 * 本地数据 / 可选云端数据 / 绝不包含的数据。
 * 静态信息卡片，无交互态。
 * 见 docs/product-specs/2026-08-05-web-marketing-site.md § Information Architecture。
 */
---

<section class="section trust grain-overlay">
  <div class="container">
    <header class="trust__header reveal">
      <h2 class="trust__title">本地优先的信任边界</h2>
      <p class="trust__lede">
        FrameQ 的核心信念：视频、音频和文字稿默认留在本机；AI 整理是单独确认的可选步骤。
      </p>
    </header>

    <div class="trust__grid">
      <article class="trust__card trust__card--local reveal" data-reveal data-reveal-delay="0">
        <header class="trust__card-header">
          <svg class="trust__card-icon trust__card-icon--local" aria-hidden="true" width="10" height="10" viewBox="0 0 10 10">
            <circle cx="5" cy="5" r="4" fill="currentColor"/>
          </svg>
          <h3 class="trust__card-title">默认留在本机</h3>
        </header>
        <ul class="trust__card-list">
          <li>视频与音频源文件</li>
          <li>转写文字稿与段落时间戳</li>
          <li>ASR 模型缓存（app-local data）</li>
          <li>历史任务记录与 AI 产物</li>
          <li>本地媒体导入的不透明选择 token</li>
        </ul>
      </article>

      <article class="trust__card trust__card--cloud reveal" data-reveal data-reveal-delay="80">
        <header class="trust__card-header">
          <svg class="trust__card-icon trust__card-icon--cloud" aria-hidden="true" width="10" height="10" viewBox="0 0 10 10">
            <circle cx="5" cy="5" r="4" fill="currentColor"/>
          </svg>
          <h3 class="trust__card-title">仅在你确认后离开</h3>
        </header>
        <ul class="trust__card-list">
          <li>AI 总结的 prompt 文本</li>
          <li>由 server-managed LLM checkout 承载</li>
          <li>每次调用独立确认、有界额度</li>
          <li>不发送视频、音频或完整文字稿</li>
        </ul>
      </article>

      <article class="trust__card trust__card--never reveal" data-reveal data-reveal-delay="160">
        <header class="trust__card-header">
          <svg class="trust__card-icon trust__card-icon--never" aria-hidden="true" width="10" height="10" viewBox="0 0 10 10">
            <circle cx="5" cy="5" r="4" fill="currentColor"/>
          </svg>
          <h3 class="trust__card-title">绝不包含</h3>
        </header>
        <ul class="trust__card-list">
          <li>安装包内置 ASR 权重</li>
          <li>LLM key 与云端 LLM 模型</li>
          <li>用户私有配置</li>
          <li>私有视频访问凭据或登录态</li>
          <li>Analytics、追踪脚本或第三方 embed</li>
        </ul>
      </article>
    </div>
  </div>
</section>

<style>
  .trust {
    background: var(--color-paper-2);
    border-block: var(--rule-hairline) solid var(--color-rule-2);
  }

  .trust__header {
    margin-bottom: var(--space-16);
    max-width: var(--content-max-narrow);
  }

  .trust__title {
    font-family: var(--font-heading);
    font-size: var(--text-3xl);
    font-weight: 500;
    margin-bottom: var(--space-3);
    font-optical-sizing: auto;
  }

  .trust__lede {
    font-size: var(--text-lg);
    color: var(--color-ink-2);
  }

  /* ---- Grid — minmax(0, 1fr) for image-bearing tracks, gate 50 ---- */
  .trust__grid {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: var(--space-6);
  }

  /* Round 1：卡片 elevation 分层。
     Round 2 P1-3：每张卡片顶部 accent 边区分语义层次，overflow hidden 配合。 */
  .trust__card {
    background: var(--card-bg);
    border: var(--rule-hairline) solid var(--card-border);
    border-radius: var(--card-radius);
    padding: var(--card-padding);
    padding-top: var(--space-5);
    display: flex;
    flex-direction: column;
    gap: var(--space-4);
    box-shadow: var(--card-shadow-rest);
    position: relative;
    overflow: hidden;
    transition:
      border-color var(--dur-fast) var(--ease-out),
      box-shadow var(--dur-fast) var(--ease-out),
      transform var(--dur-fast) var(--ease-out);
  }

  /* Round 2 P1-3：顶部 accent 边 — 通过 ::before 实现，hover 时高度增长 */
  .trust__card::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 3px;
    transition: height var(--dur-base) var(--ease-out);
  }

  .trust__card--local::before { background: var(--color-accent); }
  .trust__card--cloud::before { background: var(--color-warn); }
  .trust__card--never::before { background: var(--color-destructive); }

  .trust__card:hover {
    border-color: var(--color-rule);
    box-shadow: var(--card-shadow-hover);
    transform: translateY(-2px);
  }

  .trust__card:hover::before {
    height: 5px;
  }

  /* Round 2 P1-3："绝不包含" 卡片用更强 border 突出禁止语义 */
  .trust__card--never {
    border-color: var(--color-rule);
  }

  .trust__card-header {
    display: flex;
    align-items: center;
    gap: var(--space-3);
    padding-bottom: var(--space-3);
    border-bottom: var(--rule-hairline) solid var(--color-rule-2);
  }

  .trust__card-icon {
    flex-shrink: 0;
  }

  .trust__card-icon--local { color: var(--color-accent); }
  .trust__card-icon--cloud { color: var(--color-warn); }
  .trust__card-icon--never { color: var(--color-destructive); }

  .trust__card-title {
    font-family: var(--font-heading);
    font-size: var(--text-base);
    font-weight: 500;
    color: var(--color-ink);
  }

  .trust__card-list {
    list-style: none;
    margin: 0;
    padding: 0;
    display: flex;
    flex-direction: column;
    gap: var(--space-2);
  }

  .trust__card-list li {
    font-family: var(--font-text);
    font-size: var(--text-sm);
    color: var(--color-ink-2);
    line-height: var(--leading-snug);
    padding-left: var(--space-3);
    position: relative;
  }

  .trust__card-list li::before {
    content: '';
    position: absolute;
    left: 2px;
    top: 0.62em;
    width: 5px;
    height: 5px;
    border-radius: 50%;
    background: var(--color-muted);
  }

  /* 圆点沿用卡片顶部 accent 色，强化三栏语义分工 */
  .trust__card--local .trust__card-list li::before { background: var(--color-accent); }
  .trust__card--cloud .trust__card-list li::before { background: var(--color-warn); }
  .trust__card--never .trust__card-list li::before { background: var(--color-destructive); }

  /* ---- 移动端单列 ---- */
  @media (max-width: 768px) {
    .trust__grid {
      grid-template-columns: 1fr;
      gap: var(--space-4);
    }
  }

  /* ---- 平板端 2 列 ---- */
  @media (min-width: 769px) and (max-width: 1024px) {
    .trust__grid {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }
  }
</style>
