---
/**
 * LanguageHint — 静态语言提示组件。
 *
 * 首版仅中文，不切换。提示英文访客当前语言状态。
 * 状态覆盖：default（静态信息，无交互态）
 */
---

<aside class="lang-hint" role="note">
  <span class="lang-hint__label mono">zh-CN</span>
  <span class="lang-hint__text">当前仅提供简体中文。多语言切换是后续迭代。</span>
</aside>

<style>
  .lang-hint {
    display: inline-flex;
    align-items: center;
    gap: var(--space-2);
    padding-inline: var(--space-3);
    padding-block: var(--space-2);
    background: var(--color-paper-2);
    border: var(--rule-hairline) solid var(--color-border);
    border-radius: var(--radius-full);
    font-size: var(--text-xs);
    color: var(--color-muted);
  }

  .lang-hint__label {
    color: var(--color-ink-2);
    font-weight: 500;
  }

  .lang-hint__text {
    line-height: var(--leading-snug);
  }
</style>
