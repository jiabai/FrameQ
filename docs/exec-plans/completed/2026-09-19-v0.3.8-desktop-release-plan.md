# FrameQ v0.3.8 Desktop Release Plan

> This ExecPlan is a living document. The sections Progress, Surprises & Discoveries, Decision Log, and Outcomes & Retrospective must be kept up to date as work proceeds.

## Purpose / Big Picture

Publish the current verified desktop source as v0.3.8 so that **already-installed users** receive an upgraded `yt-dlp` and regain the ability to download YouTube videos. A user on v0.3.7 or older cannot get this from a lockfile change: their installed runtime carries the frozen `yt-dlp 2026.7.4`, and only a new installer replaces it. What a user gets that v0.3.7 did not have: YouTube videos whose media-stream fetch was being rejected with HTTP 403 now download and merge successfully, and Settings > App Updates shows "current version v0.3.8" as a read-only line so the installed version no longer has to be guessed from a filename. Local-first, packaging, privacy, and progress-contract boundaries are unchanged. The user authorized a full desktop release on 2026-09-19.

## Progress

- [x] 2026-09-19: Diagnosed the reported failure and localized it to the frozen `yt-dlp`. Validation: reproducing the worker's exact command against `https://www.youtube.com/watch?v=NOFkZBWYg5k` on the repository `.venv` (yt-dlp 2026.07.04) failed with `unable to download video data: HTTP Error 403: Forbidden`; the same command with yt-dlp 2026.08.19 downloaded and merged successfully. Network was excluded as a cause (no-proxy direct fetch to YouTube returned 200 and Google `generate_204` returned 204), and a second video reproduced the same 403.
- [x] 2026-09-19: Corrected the delivery model for the bundled runtime and confirmed the fix does not require touching packaging secrets. Validation: `scripts/build-installer.mjs:620-621` extracts only a bare CPython runtime from `FRAMEQ_PYTHON_STANDALONE_URL` (probed via `Lib`/`lib`/`pyvenv.cfg`), while `build-installer.mjs:641-652` installs dependencies at build time from `uv export --no-dev`; therefore `uv.lock` is the sole authoritative source for shipped dependencies and no archive rebuild or secret rotation is required.
- [x] 2026-09-19: Upgraded the lockfile and proved the upgrade is minimal. Validation: `uv lock --upgrade-package yt-dlp` reported `Updated yt-dlp v2026.7.4 -> v2026.8.19`; a `(name, version)` multiset comparison of the old and new lockfiles parsed from `[[package]]` blocks showed both at 160 package blocks and 3538 lines with exactly one difference, `yt-dlp 2026.7.4 -> 2026.8.19`. The remaining 46 changed lines are a representation-only normalization by uv 0.12.5 that drops redundant dependency-edge markers whose parent blocks are already forked by `resolution-markers`; the `numba` 0.62.1 and 0.65.1 blocks were spot-checked and each carries its own `resolution-markers`.
- [x] 2026-09-19: Synchronized the local environment and verified the fix end to end on the real command. Validation: `uv sync --dev` reported `+ yt-dlp==2026.8.19` (a first invocation was killed with SIGTERM and zero output; the retry succeeded, so it was environmental). Running the worker's exact command from `build_ytdlp_command` for YouTube, with the bundled Deno 2.9.1 placed on `PATH` to mirror the worker launch environment built by `app/src-tauri/src/worker_runtime/command.rs:172`, returned exit 0 and produced `NOFkZBWYg5k.mp4` at 38,749,697 B from a 26.80 MiB video stream plus a 10.07 MiB audio stream. The subtitle-carrying first attempt still fails with `HTTP Error 429` on `zh-Hans`, identical to pre-upgrade behaviour and handled by the existing drop-subtitles retry. No EJS remote-component warning appeared; the download path emitted zero warnings.
- [x] 2026-09-19: Confirmed the CI dependency path emits the upgraded pin. Validation: `uv export --no-dev --format requirements-txt` produced `yt-dlp==2026.8.19` in the generated requirements file; evidence saved to `outputs/release-038-uv-export-requirements.txt` and the two download logs to `outputs/release-038-ytdlp-verify-{nosubs,subs}.log`.
- [x] 2026-09-19: Prepared the release spec and this plan. Validation: `docs/product-specs/2026-09-19-v0.3.8-desktop-release.md` created, release notes written to `docs/releases/v0.3.8.md`, and this plan registered in `active/index.md`.
- [x] 2026-09-19: Bumped the six version sources to 0.3.8. Validation: `node scripts/check-release-version.mjs 0.3.8` passed, and the negative case (`0.3.7`) failed while listing all five version sites as expected, proving the check is not a no-op. `app/src-tauri/Cargo.lock` holds a second `0.3.7` at the `fdeflate` block, verified untouched by grepping the surrounding `name =` lines.
- [x] 2026-09-19: Ran the local release gates and recorded their logs under `outputs/release-038-*.log`. Validation: worker `858 passed / 2 skipped`, `ruff check worker` clean, app `766/766` plus `lint` and `build`, server `292 passed / 1 skipped` plus `build`, Node script tests `50/50`, docs governance `0 errors / 0 warnings` at `--level WARN`, and `git diff --check` clean.
- [x] 2026-09-19: Committed and pushed `main` and confirmed the hosted Rust evidence. Validation: `909c232` (yt-dlp fix), `3711aeb` (version display), `e5c16c4` (release prep) pushed as `52ada4d..e5c16c4`; `unix-process-supervisor.yml` run `35445654472` reported `macOS Cargo tests: completed/success` on `e5c16c4`. A follow-up docs-only commit `fc83df9` left `app/src-tauri/` byte-identical, so the evidence remained valid for the tag.
- [x] 2026-09-19: Created the annotated tag `v0.3.8` and verified the three-platform build. Validation: tag object `0db2150` dereferenced to `fc83df9`, equal to local `HEAD`; Desktop Release run `35445817959` finished `completed/success` with `Windows updater artifacts`, `macOS Intel DMG artifact`, and `macOS Apple Silicon DMG artifact` all `success`.
- [x] 2026-09-19: Published, set the body, synchronized `TASKS.md`, and archived this plan. Validation: the asset-name set matched v0.3.7 after normalizing the version; `latest.json` reported `version = 0.3.8`, carried only the two Windows platform keys, and its `windows-x86_64.signature` was byte-identical to `FrameQ_0.3.8_x64-setup.exe.sig` (both 416 characters, SHA-256 prefix `f04ed31f57f1d39a`); all five asset URLs returned `206` to `curl -sL -r 0-0`; the published body read back byte-identical to `docs/releases/v0.3.8.md` (937 characters, SHA-256 prefix `79b1ba0a36c2781d`); and the three build logs each showed `Successfully installed ... yt-dlp-2026.8.19` before a passing `import yt_dlp` smoke test, which is direct evidence that the shipped runtime carries the upgraded extractor rather than merely that the lockfile changed.
- [x] 2026-09-19: Deployed the marketing site from the rebuilt `dist`. Validation: the webroot was backed up to `/home/ubuntu/FrameQ/backups/webroot-20260919T141259Z.tar.gz` (87 entries) before any write; a `--dry-run` listed exactly 3 changed files (`index.html`, `download/index.html`, `privacy/index.html`) and 0 deletions; the apply uploaded those 3 and a re-run of `--dry-run` then reported 81 unchanged / 0 to upload / 0 to delete. Over the public host the three pages returned 200 and were byte-identical to `site/dist` (`/` `f03227f364ed7b09`, `/download/` `b333d6b7b098c131`, `/privacy/` `b841b19b1e022d56`), the live pages contain 20 occurrences of `v0.3.8` and zero of `v0.3.7` or `v0.3.6`, and all three v0.3.8 asset links plus the new release-notes body are present.

## Surprises & Discoveries

Evidence: The bundled runtime's dependency source was misread at first. `FRAMEQ_PYTHON_STANDALONE_URL` reads like a pre-baked runtime archive, but `scripts/build-installer.mjs:620-621` only extracts a bare CPython runtime from it and `build-installer.mjs:641-652` installs dependencies afterwards with `uv export --no-dev` plus `pip install -r`. The corrective conclusion is that a dependency upgrade needs a lockfile change and a new release, nothing else — no archive rebuild, no re-upload, no secret rotation. Only a CPython major-version change or a new ffmpeg/Deno asset would require touching those secrets.

Evidence: `uv lock --upgrade-package yt-dlp` rewrote 51 lines, most of which had nothing to do with `yt-dlp`. The local toolchain is uv 0.12.5 while the committed lockfile carries `revision = 3`, and the newer version normalizes dependency-edge markers away when the parent package block is already forked by `resolution-markers` (the edge marker is then exactly the complement and therefore redundant). Real examples: `coloredlogs -> humanfriendly`, `numba -> llvmlite/numpy`, and `cuda-pathfinder`. A `(name, version)` multiset comparison over `[[package]]` blocks is the reliable way to tell normalization apart from a genuine resolution change, and it showed the package set identical at 160 blocks on both sides.

Evidence: `uv sync --dev` was killed with SIGTERM and produced no output on its first invocation in this session, then succeeded unchanged on retry (`+ yt-dlp==2026.8.19`). The same pattern was recorded for the v0.3.7 release, where the project `.venv` had to be rebuilt and a build-backend cleanup was intercepted once. Treat a silent SIGTERM here as environmental and retry before investigating.

Evidence: The pre-existing YouTube subtitle path is rate-limited independently of the `yt-dlp` version. On both the old and new releases, the first attempt with `--write-subs --write-auto-subs --sub-langs zh-Hans,...` fails with `Unable to download video subtitles for 'zh-Hans': HTTP Error 429: Too Many Requests`, and only the worker's drop-subtitles retry reaches the media stream. This is why a fix that only touches the media-stream fetch step is sufficient to restore YouTube downloads, and also why the subtitle attempt still costs a wasted round trip per task.

Evidence: A new `impersonation` warning appears on the subtitle path under yt-dlp 2026.8.19 — `The extractor specified to use impersonation for this download, but no impersonate target is available` — and no EJS remote-component requirement (`--remote-components ejs:github`) surfaced for the tested video. The impersonation warning is benign here because it precedes a failure that already has a fallback, but it is a forward-looking fragility worth watching if a future yt-dlp starts requiring an impersonation target on the media-stream path too.

Evidence: `cargo` is not reachable from this session, so the Rust side of the version-display change cannot be compiled locally. However `.github/workflows/unix-process-supervisor.yml` is path-filtered on `app/src-tauri/**` and runs on `push` to `main`, executing `cargo test --manifest-path app/src-tauri/Cargo.toml` on `macos-latest`. Pushing `main` before tagging therefore produces the Rust compile and test evidence that this session cannot, and sequencing the release that way de-risks the tag.

Evidence: `desktop-release.yml` triggers on `push: tags: v*` while `macos-intel-acceptance.yml` is `workflow_dispatch`-only, so tagging alone builds the three-platform draft without producing the Intel acceptance run. `server-ci.yml` is path-filtered on `server/**`, `deploy/**`, and named docs, so a version bump and a lockfile change do not trigger it.

## Decision Log

Decision: Release as v0.3.8 and ship the `yt-dlp` upgrade together with the read-only current-version display. Rationale: the version display is the only practical way for a user to confirm that the upgraded runtime actually landed in their installed build, so the two changes are complementary rather than merely adjacent; the display reuses an existing command and its value is the compile-time crate version, so it adds no drift risk. Date/Author: 2026-09-19 / User + Alex.

Decision: Fix the dependency problem at the lockfile instead of patching the worker. Rationale: `uv.lock` is the sole authoritative source for what the installer bundles, and `yt-dlp` is invoked as a library module (`sys.executable -m yt_dlp`), so pinning or vendoring a different copy in the worker would create a second source of truth for the same tool. Date/Author: 2026-09-19 / Alex.

Decision: Leave the YouTube failure-classification gap out of this release and keep it as tracked debt. Rationale: 403 and 429 currently fall into the generic `YOUTUBE_DOWNLOAD_FAILED` bucket whose copy tells the user to check their network, which is genuinely misleading, but fixing it spans worker classification, the desktop-worker contract, and three locales. Widening the release would delay the fix that unblocks installed users, which is the whole point of this version. Date/Author: 2026-09-19 / User + Alex.

Decision: Push `main` and wait for the hosted macOS Cargo tests before creating the tag. Rationale: the Rust addition to `updates.rs` cannot be compiled in this session, and the tag triggers a long three-platform build whose failure would waste roughly an hour; `unix-process-supervisor.yml` provides the missing compile evidence cheaply on the release commit. Date/Author: 2026-09-19 / Alex.

Decision: Do not deploy production server or migrate its database as part of this desktop publication. Rationale: this release changes no server source, and the server was already deployed, migrated, and end-to-end verified on 2026-09-19. Date/Author: 2026-09-19 / Alex.

## Outcomes & Retrospective

Outcome: published as v0.3.8 on 2026-09-19 at 22:10:06 +08:00 from tag target `fc83df9`, released at https://github.com/jiabai/FrameQ/releases/tag/v0.3.8.

User-visible behaviour that shipped: YouTube downloads that were failing with HTTP 403 now complete, because the installed runtime bundles `yt-dlp 2026.8.19` instead of `2026.7.4`, and Settings > App Updates shows a read-only "current version v0.3.8" line whose value is the compile-time crate version the updater itself compares against. Nothing else changed: no new IPC surface beyond one extra field on an existing response, no new stage, no packaging boundary change.

Release commit and tag: `909c232` (yt-dlp), `3711aeb` (version display), `e5c16c4` (release prep), `fc83df9` (docs correction); annotated tag `v0.3.8` (`0db2150`) dereferences to `fc83df9`.

Desktop Release run `35445817959`: `Windows updater artifacts` success, `macOS Intel DMG artifact` success, `macOS Apple Silicon DMG artifact` success, ~41 minutes wall clock. Published assets: `FrameQ_0.3.8_x64-setup.exe` (269,528,912 B), `FrameQ_0.3.8_x64-setup.exe.sig` (416 B), `FrameQ_0.3.8_x64.dmg` (533,042,191 B), `FrameQ_0.3.8_aarch64.dmg` (418,890,096 B), `latest.json` (1,300 B). The name set is identical to v0.3.7 after normalizing the version. `latest.json` was cross-checked at the byte level: its `windows-x86_64.signature` equals the uploaded `.sig` file exactly (416 characters, SHA-256 prefix `f04ed31f57f1d39a`), it reports `version = 0.3.8`, and it carries only the two Windows platform keys, which matches the standing macOS-by-manual-DMG convention rather than being a defect. Every asset URL answered `206` to a one-byte ranged request. The published body is byte-identical to `docs/releases/v0.3.8.md` (937 characters, SHA-256 prefix `79b1ba0a36c2781d`).

Strongest evidence for the actual fix: the three build logs each contain `Successfully installed ... yt-dlp-2026.8.19` immediately followed by a passing `import funasr, funasr_onnx, modelscope, onnxruntime, yt_dlp` smoke test against the bundled runtime. This is the check that closes the loop the lockfile comparison could not: it observes the shipped interpreter rather than the repository.

Marketing site: `LATEST_VERSION` and `RELEASE_NOTES_PATH` moved to v0.3.8, `site/src/content/release-notes/v0.3.8.md` added, `npm run build` produced three pages, and the built HTML contains zero occurrences of the previous version strings with all three v0.3.8 download URLs present. Deployment followed the recorded procedure: backup the webroot, preview with `--dry-run`, apply, then re-run `--dry-run` and expect zero diffs. The live pages are byte-identical to the local build and the webroot now reports 81 unchanged files with nothing to upload or delete. Credentials were used only as in-memory environment variables and were never written to a file, a log, or the release record.

Retrospective: the expensive mistake in this release was the first diagnosis of the delivery mechanism, not the fix. Claiming that reaching installed users required rebuilding the python-standalone archive and rotating `FRAMEQ_PYTHON_STANDALONE_URL` would have turned a one-line lockfile change into a packaging-asset exercise and, worse, would have sent the next engineer to a secret that is not involved. Reading `build-installer.mjs` end to end is what corrected it, and it is why the plan now spells out that the secret holds bare CPython only. Two smaller lessons: comparing lockfiles as a `(name, version)` multiset is the only cheap way to separate a toolchain normalization from a real resolution shift, and pushing `main` first to harvest hosted Rust compile evidence is worth the extra round trip on any release that touches `app/src-tauri`, since this workstation has no `cargo`.

Residual risks carried forward: `desktop-release.yml` still does not set the release body, so a body must always be written after publishing; `macos-intel-acceptance.yml` is `workflow_dispatch`-only and did not run on this tag; clean-machine Windows and macOS UI smoke was not repeated for v0.3.8; macOS builds remain ad-hoc signed without notarization; the YouTube classification gap is not fixed, so a 403 or 429 still surfaces as the generic `YOUTUBE_DOWNLOAD_FAILED` whose copy tells the user to check a healthy network, and `yt-dlp 2026.8.19` emits a benign `impersonation` warning on the subtitle attempt that would become a real problem if a future version requires an impersonate target on the media-stream path; public-platform availability and deployed server capability remain external dependencies. The manual regression step below — installing the Windows x64 build and processing a previously failing YouTube URL from the installed app — had not been performed at the time this plan was archived.

## Context and Orientation

- Spec: `docs/product-specs/2026-09-19-v0.3.8-desktop-release.md`.
- Notes: `docs/releases/v0.3.8.md`.
- Dependency source of truth: `uv.lock` (`yt-dlp`), `pyproject.toml` (`yt-dlp>=2026.6.9`).
- Installer dependency install path: `scripts/build-installer.mjs` (`uv export --no-dev`, `pip install -r`, Python runtime smoke test).
- Worker download command: `worker/frameq_worker/media.py` (`build_ytdlp_command`, `YOUTUBE_FORMAT_SELECTOR`, `YOUTUBE_JS_RUNTIMES`, `PLATFORM_SUBTITLE_ARGS`, `classify_youtube_download_failure`).
- Rust PATH injection for bundled Deno: `app/src-tauri/src/worker_runtime/command.rs` (`prepend_to_path` call sites; `runtime.rs` holds only the helper).
- Version display: `app/src-tauri/src/updates.rs`, `app/src/updateClient.ts`, `app/src/features/updates/useAppUpdateController.ts`, `app/src/features/settings/SettingsSheet.tsx`, `app/src/i18n/updatesResources.ts`.
- Versions: `app/package.json`, `app/package-lock.json`, `app/src-tauri/tauri.conf.json`, `app/src-tauri/Cargo.toml`, `app/src-tauri/Cargo.lock`, `scripts/tests/release-version.test.mjs`.
- Workflows: `.github/workflows/desktop-release.yml`, `.github/workflows/unix-process-supervisor.yml`, `.github/workflows/server-ci.yml`, `.github/workflows/macos-intel-acceptance.yml`.
- Prior release template: `docs/exec-plans/completed/2026-09-19-v0.3.7-desktop-release-plan.md`.
- Evidence logs: `outputs/release-038-*.log`, `outputs/release-038-uv-export-requirements.txt`.

## Plan of Work

1. Write the release spec, this plan, and `docs/releases/v0.3.8.md`.
2. Bump the six version sources to 0.3.8 and run `node scripts/check-release-version.mjs 0.3.8`.
3. Run the local release gates: worker pytest and Ruff, app tests/lint/build, server tests/build, Node script tests, docs governance, and `git diff --check`.
4. Commit, push `main`, and confirm the hosted macOS Cargo tests pass on that commit.
5. Create and push the annotated tag `v0.3.8`; verify the three draft artifact jobs and their assets.
6. Publish the release, set its body from `docs/releases/v0.3.8.md`, synchronize `TASKS.md`, archive this plan, and sync the marketing site.

## Validation and Acceptance

Run `node scripts/check-release-version.mjs 0.3.8`; Node script tests via `node --test scripts/tests/*.test.mjs`; worker gates via `uv run pytest worker/tests` and `uv run ruff check worker`; app gates via `npm --prefix app test`, `npm --prefix app run lint`, and `npm --prefix app run build`; server gates via `npm --prefix server test` and `npm --prefix server run build`; docs governance via `python scripts/validate_agents_docs.py --level WARN`; and `git diff --check`. Hosted `unix-process-supervisor.yml` supplies the Rust compile and test evidence; Desktop Release validates the three-platform artifacts. Manual regression step: install the Windows x64 build of v0.3.8, confirm Settings > App Updates shows "current version v0.3.8", and process one YouTube URL that failed before the upgrade, confirming it downloads, transcribes, and exports. Evidence: the local release logs under `outputs/release-038-*.log`, the release-commit CI run IDs, and the Desktop Release run ID.
