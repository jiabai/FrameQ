# FrameQ v0.3.6 Desktop Release Plan

> This ExecPlan is a living document. The sections Progress, Surprises & Discoveries, Decision Log, and Outcomes & Retrospective must be kept up to date as work proceeds.

## Purpose / Big Picture

Publish current verified desktop changes as v0.3.6, including direct Xiaohongshu subtitle reuse, without changing local-first or packaging boundaries. User authorized committing all local changes, pushing main and publishing a new release on 2026-09-05.

## Progress

- [x] 2026-09-05: Inspected local changes, latest Release and remote main. Validation: latest v0.3.5 at 2ce6706; local and remote main at 5001931, 53 commits ahead of release; existing Rust status entries have no text diff.
- [x] 2026-09-05: Prepared 0.3.6 version sources, release scope and notes. Validation: `node scripts/check-release-version.mjs 0.3.6` passed.
- [x] 2026-09-05: Worker 854 passed / 2 skipped and Ruff passed; app 762 passed, lint/build passed; server 291 passed / 1 skipped and build passed; scripts 37 passed; governance 0 errors/warnings. Validation: local release logs under ignored `outputs/release-036-*.log`; memory-pressure failures disappeared on isolated reruns.
- [x] 2026-09-05: Rust isolated full run passed. Validation: `cargo test --manifest-path app/src-tauri/Cargo.toml -j 1 -- --test-threads=1`: 315 passed, 0 failed; main/doc tests passed.
- [ ] Complete hosted release gates.
- [ ] Commit and push main and annotated release tag; build three-platform draft artifacts.
- [ ] Publish stable release, update task ledger and archive plan, verify runtime source parity.

## Surprises & Discoveries

Evidence: The numeric-format fix and subtitle downloader are newer than v0.3.5. A real subtitle-only call returned 262 en-US segments, identical to directly fetched SRT, without loading ASR libraries.

Evidence: Concurrent local Rust/frontend/worker execution exhausted memory; isolated worker and frontend reruns passed without source changes. Rust rerun uses `-j 1 -- --test-threads=1`.

Evidence: Project policy requires post-publication ledger synchronization. Any ledger-only follow-up commit must leave released runtime sources byte-identical and must not move an already published tag.

## Decision Log

Decision: Use v0.3.6 and retain draft-first three-platform build workflow. Rationale: next available patch release, existing distribution process and explicit user authorization. Date/Author: 2026-09-05 / User + Codex.

Decision: Do not deploy production server or migrate its database as part of desktop publication. Rationale: release includes compatible client behavior; production deployment is a separate operation. Date/Author: 2026-09-05 / Codex.

## Outcomes & Retrospective

Pending validation and publication. Residual risk: clean-machine Windows and macOS native UI smoke are not yet performed for this release; public-platform availability and deployed server capability remain external dependencies.

## Context and Orientation

- Spec: `docs/product-specs/2026-09-05-v0.3.6-desktop-release.md`.
- Notes: `docs/releases/v0.3.6.md`.
- Workflow: `.github/workflows/desktop-release.yml`, `.github/workflows/server-ci.yml`.
- Versions: app package/lock, Tauri config, Cargo manifest/lock.

## Plan of Work

1. Inspect and archive outstanding local docs; update release versions.
2. Run worker/app/server/Rust/script and governance checks; resolve actionable failures.
3. Push main, verify hosted gates, push v0.3.6 tag, verify draft assets.
4. Publish reviewed notes, synchronize ledger and completed plan, verify clean main and released runtime parity.

## Validation and Acceptance

Run release-version checker; Node script tests; worker pytest/Ruff; app tests/lint/build; server tests/build; Rust tests; governance and git diff checks. Hosted Server CI validates migrations/preflight/restore; Desktop Release validates three-platform artifacts; macOS ProcessSupervisor covers macOS process gates. Record actual results before publication.
