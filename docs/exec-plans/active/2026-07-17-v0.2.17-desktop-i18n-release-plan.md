# FrameQ v0.2.17 Desktop i18n Release Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Prepare one verified v0.2.17 release commit for the completed desktop i18n and AI output-language work without creating a tag or publishing a Release.

**Architecture:** Release preparation changes metadata, documentation, and one existing frontend quality-gate entry point only. Runtime behavior and desktop-worker contract remain unchanged; GitHub-hosted platform builds stay behind the later tag-triggered Draft Release checkpoint.

**Tech Stack:** Node.js release scripts, npm, TypeScript, Vitest, Tauri/Rust, Python/uv/pytest/Ruff, GitHub Actions, Markdown governance documents.

---

## Progress

- [x] 2026-07-17: Audited `main`, confirmed it is clean and synchronized with `origin/main`, and
  identified version metadata, release notes, lint command, and hosted artifact evidence as the
  remaining release-preparation gaps.
- [x] 2026-07-17: Approved v0.2.17 scope as the completed desktop i18n/output-language release while
  excluding unimplemented local-media import and other documentation-only future features.
- [x] 2026-07-17: Added RED release-version and app-lint command contract tests. The focused version
  test failed because all five sources reported `0.2.16`; the lint test failed because the script was
  absent.
- [x] 2026-07-17: Synchronized version metadata, implemented the lint entry point, and made the two
  focused tests, explicit version checker, and app lint command GREEN.
- [x] 2026-07-17: Ran all local release gates, recorded evidence and residual risk, and left the diff
  ready for review without tagging or publishing.
- [x] 2026-07-18: Changed the no-preference desktop default to explicit `en-US` while preserving every
  saved user preference. RED tests proved the previous frontend and Rust defaults were `system`;
  focused tests, 491 frontend tests, app lint/build, 134 Rust tests, and rustfmt then passed.
- [x] 2026-07-22: Reclassified broad consumer publication as blocked on atomic authoritative
  persistence and a Rust-owned worker watchdog. Product/design documents and two independent active
  ExecPlans now define the required implementation and validation; neither blocker is implemented
  by this documentation update. Validation: governance indexes, release gates, debt tracker, and
  code-audit metadata were synchronized; documentation validation is recorded in the handoff.
- [x] 2026-07-22: Atomic persistence was reviewed and fast-forward merged to `main` at `61d489a`.
  Worker 563 passed / 2 skipped, App 551 passed, Windows Rust 185 passed, scripts 25 passed,
  packaged-worker 63-file equality and Tauri `--no-bundle` passed. The completed plan is
  `docs/exec-plans/completed/2026-07-22-atomic-persistence-hardening-plan.md`; macOS/Unix native and
  real Tauri forced-exit smoke remain explicitly unverified residual risks.
- [x] 2026-07-22: Implemented and accepted
  `docs/exec-plans/completed/2026-07-22-worker-watchdog-plan.md`. Rust 208/208, App 567/567,
  worker 563 passed / 2 skipped, scripts 25/25, Chromium smoke 28/28, lint, Ruff, rustfmt, build,
  and Tauri `--no-bundle` passed. Native Windows watchdog termination removed a parent and
  descendant and admitted a second task. The portable macOS fixture is workflow-enforced but was
  not run on a macOS host in this session.
- [x] 2026-07-22: Reviewed and specified the remaining server release blocker as one durable design
  plus separate authentication/quota-concurrency and production-operations active ExecPlans; no
  server code or live database changed. Validation: governance validation reported 0 errors and 0
  warnings, and tracked plus new-document whitespace checks passed.
- [ ] Implement and accept both active server authentication/quota concurrency and production-
  operations hardening plans before broad publication.

## Decision Log

- Decision: Use `0.2.17` as a patch release. Rationale: the release packages a completed additive UI
  localization feature and compatible strict worker contract without changing the product's major
  distribution or billing model. Date/Author: 2026-07-17, User + Codex.
- Decision: Define `npm --prefix app run lint` as TypeScript no-emit validation plus the existing
  production i18n-literal boundary check. Rationale: both checks already express current frontend
  quality policy; introducing a new ESLint ruleset immediately before release would create unrelated
  migration risk. Date/Author: 2026-07-17, Codex.
- Decision: Keep tag creation, hosted Draft generation, and public publication outside this execution.
  Rationale: each is an externally visible checkpoint that follows review of the release-prep diff.
  Date/Author: 2026-07-17, User + Codex.
- Decision: New installs and preference recovery use explicit `en-US`; existing saved preferences are
  never migrated, and `system` remains selectable. Rationale: App Settings > Basic must initially
  select `English` without overriding current users. Date/Author: 2026-07-18, User + Codex.
- Decision: Do not tag or broadly publish v0.2.17 until atomic persistence, worker watchdog, and both
  server authentication/quota plus production-operations plans have acceptance evidence. Rationale:
  clean-machine build success does not prove crash consistency, bounded worker execution, database
  concurrency correctness, or production operability. Date/Author: 2026-07-22, User + Codex.
- Decision: Keep server database correctness and production operations as two active ExecPlans under
  one durable design. Rationale: OTP/ticket/quota schema transactions and logger/health/shutdown/
  restore deployment changes need different failure injection and review checkpoints, while broad
  publication still requires both. Date/Author: 2026-07-22, User + Codex.

## Outcomes & Retrospective

The original i18n release-preparation implementation is complete. All desktop version sources now report `0.2.17`,
new installs and preference recovery select explicit `English` without migrating saved preferences,
the documented app lint command is executable without introducing a new lint framework, and the
reviewed release notes keep macOS trust limitations ahead of downloads. Desktop atomic persistence
and worker watchdog hardening are complete; broad publication remains blocked on the two active
server authentication/quota and production-operations plans. Unavailable macOS watchdog runtime and
real desktop interaction evidence remain recorded residual risks. Tag creation, hosted Draft
artifacts, native macOS locale/layout acceptance, and public publication remain pending checkpoints.

## Context and Orientation

- Release policy: `docs/product-specs/2026-07-17-v0.2.17-desktop-i18n-release.md`.
- Included feature: `docs/exec-plans/completed/2026-07-15-desktop-i18n-ai-output-language-plan.md`.
- Previous release precedent: `docs/exec-plans/completed/2026-07-12-v0.2.16-open-source-release-plan.md`.
- Version guard: `scripts/check-release-version.mjs` and `scripts/tests/release-version.test.mjs`.
- Frontend quality gates: `app/package.json`, `app/tsconfig.json`, and
  `scripts/check-i18n-literals.mjs`.
- Hosted build: `.github/workflows/desktop-release.yml`.
- Concurrent future work: `docs/exec-plans/active/2026-07-16-local-media-file-import-plan.md` remains
  documentation-only and outside this release.
- Broad-release reliability specification:
  `docs/product-specs/2026-07-22-release-reliability-hardening.md`.
- Atomic persistence blocker:
  `docs/exec-plans/completed/2026-07-22-atomic-persistence-hardening-plan.md`.
- Completed worker watchdog:
  `docs/exec-plans/completed/2026-07-22-worker-watchdog-plan.md`.
- Server hardening design:
  `docs/design-docs/2026-07-22-server-auth-quota-operations-hardening.md`.
- Active server correctness and operations plans:
  `docs/exec-plans/active/2026-07-22-server-auth-quota-concurrency-hardening-plan.md` and
  `docs/exec-plans/active/2026-07-22-server-production-operations-hardening-plan.md`.

## Plan of Work

### Task 1: Publish Release Intent and Scope

**Files:**
- Create: `docs/product-specs/2026-07-17-v0.2.17-desktop-i18n-release.md`
- Create: `docs/releases/v0.2.17.md`
- Modify: `docs/product-specs/index.md`
- Modify: `docs/exec-plans/active/index.md`
- Modify: `TASKS.md`
- Modify: `AGENTS.md`

- [x] Define included/excluded features, three-platform artifacts, unchanged macOS trust policy, and
  draft-first publication checkpoints.
- [x] Draft release notes with Gatekeeper disclosure before download links and no claims for
  unimplemented local-media import.
- [x] Register this plan without closing the concurrent local-media planning track.

### Task 2: Establish RED Release Gates

**Files:**
- Modify: `scripts/tests/release-version.test.mjs`
- Create: `scripts/tests/app-lint-script.test.mjs`

- [x] Change the repository version expectation to `0.2.17`, run the focused test, and verify it fails
  because all five sources still report `0.2.16`.
- [x] Add a package-script contract test requiring `tsc --noEmit` and
  `node ../scripts/check-i18n-literals.mjs`, run it, and verify it fails because `lint` is absent.

### Task 3: Synchronize Metadata and Lint

**Files:**
- Modify: `app/package.json`
- Modify: `app/package-lock.json`
- Modify: `app/src-tauri/Cargo.toml`
- Modify: `app/src-tauri/Cargo.lock`
- Modify: `app/src-tauri/tauri.conf.json`

- [x] Set every desktop version source to `0.2.17` without changing dependency versions.
- [x] Add the minimal app lint script required by the RED contract test.
- [x] Run the two focused script tests, the version checker, and the app lint command; require GREEN.

### Task 4: Verify the Release-Prep Diff

**Files:**
- Modify: `docs/exec-plans/active/2026-07-17-v0.2.17-desktop-i18n-release-plan.md`

- [x] Run script, app, Rust, worker, server, documentation, build, and diff gates listed below.
- [x] Run `npm --prefix app run tauri -- build --no-bundle` after refreshing the packaged worker
  through the established Tauri dev/release path.
- [x] Record exact test totals, warnings, unavailable native evidence, and the final worktree state.
- [x] Do not create a tag, run Desktop Release, or publish a GitHub Release in this task.

### Task 5: Close Broad-Release Reliability Blockers

**Files:**
- Modify: this release plan with accepted evidence from each independent ExecPlan

- [x] Accepted atomic persistence after complete failure-injection, cross-language recovery,
  packaging-mirror, and available native filesystem evidence was recorded; unavailable macOS/Unix
  and real-Tauri cases remain explicit residual risk.
- [x] Accepted worker watchdog after deterministic race/process-tree tests, complete local gates,
  native Windows timeout evidence, and explicit recording of unavailable macOS runtime evidence.
- [ ] Accept the server authentication/quota plan after independent-client SQLite concurrency,
  rollback, idempotency, migration, and non-echoing evidence passes.
- [ ] Accept the server production-operations plan after fail-closed configuration, redacted logs,
  trusted proxy, health, graceful shutdown, backup/restore, hosted CI, and required staging evidence
  passes.
- [ ] Re-run the complete release gate after all blockers merge; do not reuse the 2026-07-17 totals
  as evidence for the later runtime changes.

## Validation and Acceptance

```powershell
node --test scripts\tests\*.test.mjs
node scripts\check-release-version.mjs 0.2.17
npm --prefix app test
npm --prefix app run lint
npm --prefix app run build
cargo test --manifest-path app\src-tauri\Cargo.toml
cargo fmt --manifest-path app\src-tauri\Cargo.toml -- --check
uv run pytest worker\tests
uv run ruff check worker
npm --prefix server test
npm --prefix server run build
python scripts\validate_agents_docs.py --level WARN
node --input-type=module -e "import { prepareFreshWorkerResource } from './scripts/tauri-dev-fresh-worker.mjs'; await prepareFreshWorkerResource();"
npm --prefix app run tauri -- build --no-bundle
git diff --check
git status --short
```

Local evidence recorded on 2026-07-17:

- Script tests: 23/23 passed; the explicit version checker reported consistent `0.2.17` sources.
- App: 62 files / 488 tests passed; lint passed TypeScript and i18n literal checks; production build
  passed. Vite retained the known 653.85 kB main-chunk warning.
- Rust: 134/134 tests and rustfmt passed for `app v0.2.17`.
- Worker: 363/363 tests and Ruff passed. PyDub retained the known Python 3.13 `audioop` deprecation
  warning.
- Server: 12 files / 57 tests and TypeScript build passed. Node retained its experimental SQLite
  warning.
- Governance validation passed after adding the repository-required `✅ Acceptance` marker to the
  release task.
- The refreshed packaged worker matched all 28 canonical source files by SHA-256, and Tauri
  `--no-bundle` built `app/src-tauri/target/release/app.exe` successfully.

Hosted Draft acceptance remains pending until a later approved tag action:

- Desktop Release succeeds for all three platform jobs from one tagged commit.
- Windows updater artifacts, Intel DMG, and Apple Silicon DMG all carry version `0.2.17`.
- Runtime import, bundled Deno, updater metadata, and macOS codesign checks pass.
- Reviewed v0.2.17 release notes replace the generic generated body before publication.
